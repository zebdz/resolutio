// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider      = "prisma-client-js"
  output        = "../generated/prisma"
  engineType    = "client"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}

// Enums
enum PollState {
  DRAFT
  READY
  ACTIVE
  FINISHED
}

enum OverallRisk {
  low
  medium
  high

  @@map("overall_risk_enum")
}

// Authentication Models
model User {
  id          String   @id @default(cuid())
  firstName   String   @map("first_name")
  lastName    String   @map("last_name")
  middleName  String?  @map("middle_name")
  phoneNumber String   @unique @map("phone_number")
  password    String
  language              String    @default("ru")
  consentGivenAt        DateTime? @map("consent_given_at")
  createdAt             DateTime  @default(now()) @map("created_at")
  nickname              String    @unique
  allowFindByName       Boolean   @default(false) @map("allow_find_by_name")
  allowFindByPhone      Boolean   @default(false) @map("allow_find_by_phone")
  privacySetupCompleted Boolean   @default(false) @map("privacy_setup_completed")
  confirmedAt           DateTime? @map("confirmed_at")
  allowFindByAddress    Boolean   @default(false) @map("allow_find_by_address")

  // Relations
  sessions           Session[]
  organizationsOwned Organization[]
  memberships        OrganizationUser[]
  adminRoles         OrganizationAdminUser[]
  boardMemberships   BoardUser[]
  boardMembersAdded  BoardUser[]        @relation("AddedBoardMember")
  boardMembersRemoved BoardUser[]       @relation("RemovedBoardMember")
  votes              Vote[]
  pollsCreated       Poll[]
  superAdmin         SuperAdmin?
  acceptedRequests   OrganizationUser[] @relation("AcceptedByAdmin")
  rejectedRequests   OrganizationUser[] @relation("RejectedByAdmin")
  pollParticipants   PollParticipant[]
  voteDrafts         VoteDraft[]
  weightChanges      ParticipantWeightHistory[] @relation("WeightChangedBy")
  requestedParentJoinRequests OrganizationJoinParentRequest[] @relation("RequestingAdmin")
  handledParentJoinRequests   OrganizationJoinParentRequest[] @relation("HandlingAdmin")
  notifications              Notification[]
  privacyAuditLogs           UserPrivacyAuditLog[]
  rateLimitEvents            RateLimitEvent[]
  blockStatuses              UserBlockStatus[] @relation("BlockedUser")
  changedUserBlockStatuses   UserBlockStatus[] @relation("StatusChangedBySuperadmin")
  changedIpBlockStatuses     IpBlockStatus[]   @relation("IpBlockStatusChangedBy")
  otpVerifications           OtpVerification[]
  invitationsSent            Invitation[] @relation("InvitationInviter")
  invitationsReceived        Invitation[] @relation("InvitationInvitee")
  joinTokensCreated          OrganizationJoinToken[] @relation("JoinTokenCreator")
  address            Address?
  legalChecks        PollLegalCheck[]    @relation("LegalChecks")
  legalCheckLogs     PollLegalCheckLog[] @relation("LegalCheckLogs")
  propertyOwnerships PropertyAssetOwnership[]
  pollEligibilities  PollEligibleMember[]
  propertyClaimsSubmitted PropertyClaim[] @relation("PropertyClaimUser")
  propertyClaimsDecided   PropertyClaim[] @relation("PropertyClaimDecidedBy")

  @@map("users")
}

model Address {
  id         String   @id @default(cuid())
  userId     String   @unique @map("user_id")
  country    String
  region     String?
  city       String
  street     String
  building   String
  apartment  String?
  postalCode String?  @map("postal_code")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("addresses")
}

model Session {
  id        String   @id
  userId    String   @map("user_id")
  expiresAt DateTime @map("expires_at")
  createdAt DateTime @default(now()) @map("created_at")
  ipAddress String?  @map("ip_address")
  userAgent String?  @map("user_agent")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("sessions")
}

model SuperAdmin {
  userId    String   @id @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("superadmins")
}

// Organization Models
model Organization {
  id          String   @id @default(cuid())
  name        String   @unique
  description String
  parentId    String?  @map("parent_id")
  createdById String   @map("created_by_id")
  createdAt   DateTime @default(now()) @map("created_at")
  archivedAt  DateTime? @map("archived_at")
  allowMultiTreeMembership Boolean? @default(false) @map("allow_multi_tree_membership")

  // Relations
  createdBy           User                    @relation(fields: [createdById], references: [id])
  parent              Organization?           @relation("OrganizationHierarchy", fields: [parentId], references: [id])
  children            Organization[]          @relation("OrganizationHierarchy")
  members             OrganizationUser[]
  admins              OrganizationAdminUser[]
  boards              Board[]
  polls               Poll[]
  childJoinRequests   OrganizationJoinParentRequest[] @relation("ChildOrgJoinParent")
  parentJoinRequests  OrganizationJoinParentRequest[] @relation("ParentOrgJoinParent")
  invitations         Invitation[]
  joinTokens          OrganizationJoinToken[]
  properties          OrganizationProperty[]
  propertyClaims      PropertyClaim[]

  @@index([parentId])
  @@index([createdById])
  @@map("organizations")
}

model OrganizationUser {
  id                String    @id @default(cuid())
  organizationId    String    @map("organization_id")
  userId            String    @map("user_id")
  status            String    @default("pending") // "pending" | "accepted" | "rejected"
  createdAt         DateTime  @default(now()) @map("created_at")
  acceptedAt        DateTime? @map("accepted_at")
  rejectedAt        DateTime? @map("rejected_at")
  rejectionReason   String?   @map("rejection_reason")
  acceptedByUserId  String?   @map("accepted_by_user_id")
  rejectedByUserId  String?   @map("rejected_by_user_id")
  joinTokenId       String?   @map("join_token_id")

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  joinToken    OrganizationJoinToken? @relation(fields: [joinTokenId], references: [id])
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  acceptedBy   User?        @relation("AcceptedByAdmin", fields: [acceptedByUserId], references: [id], onDelete: SetNull)
  rejectedBy   User?        @relation("RejectedByAdmin", fields: [rejectedByUserId], references: [id], onDelete: SetNull)

  @@unique([organizationId, userId])
  @@index([userId])
  @@index([acceptedByUserId])
  @@index([rejectedByUserId])
  @@map("organization_users")
}

model OrganizationJoinToken {
  id             String    @id @default(cuid())
  organizationId String    @map("organization_id")
  token          String    @unique
  description    String
  maxUses        Int?      @map("max_uses")
  useCount       Int       @default(0) @map("use_count")
  createdById    String    @map("created_by_id")
  createdAt      DateTime  @default(now()) @map("created_at")
  expiredAt      DateTime? @map("expired_at")

  organization Organization @relation(fields: [organizationId], references: [id])
  createdBy    User         @relation("JoinTokenCreator", fields: [createdById], references: [id])
  usages       OrganizationUser[]

  @@index([organizationId])
  @@map("organization_join_tokens")
}

model OrganizationAdminUser {
  organizationId String   @map("organization_id")
  userId         String   @map("user_id")
  createdAt      DateTime @default(now()) @map("created_at")

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([organizationId, userId])
  @@index([userId])
  @@map("organization_admin_users")
}

// Board Models
model Board {
  id             String    @id @default(cuid())
  name           String
  organizationId String    @map("organization_id")
  createdAt      DateTime  @default(now()) @map("created_at")
  archivedAt     DateTime? @map("archived_at")

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  members      BoardUser[]
  polls        Poll[]
  invitations  Invitation[]

  @@unique([name, organizationId])
  @@index([organizationId])
  @@map("boards")
}

model BoardUser {
  id            String    @id @default(cuid())
  boardId       String    @map("board_id")
  userId        String    @map("user_id")
  addedBy       String?   @map("added_by")
  addedAt       DateTime  @default(now()) @map("added_at")
  removedBy     String?   @map("removed_by")
  removedAt     DateTime? @map("removed_at")
  removedReason String?   @map("removed_reason")
  createdAt     DateTime  @default(now()) @map("created_at")

  board         Board @relation(fields: [boardId], references: [id], onDelete: Cascade)
  user          User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  addedByUser   User? @relation("AddedBoardMember", fields: [addedBy], references: [id], onDelete: SetNull)
  removedByUser User? @relation("RemovedBoardMember", fields: [removedBy], references: [id], onDelete: SetNull)

  @@unique([boardId, userId])
  @@index([userId])
  @@index([addedBy])
  @@index([removedBy])
  @@map("board_users")
}

// Poll Models
model Poll {
  id             String    @id @default(cuid())
  title          String
  description    String
  organizationId String    @map("organization_id")
  boardId        String?   @map("board_id")
  startDate      DateTime  @map("start_date")
  endDate        DateTime  @map("end_date")
  state                PollState @default(DRAFT)
  weightCriteria       String?   @map("weight_criteria")
  distributionType     String    @default("EQUAL")   @map("distribution_type")
  propertyAggregation  String    @default("RAW_SUM") @map("property_aggregation")
  createdBy      String    @map("created_by")
  createdAt      DateTime  @default(now()) @map("created_at")
  archivedAt     DateTime? @map("archived_at")

  organization    Organization                    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  board           Board?                          @relation(fields: [boardId], references: [id], onDelete: Cascade)
  creator         User                            @relation(fields: [createdBy], references: [id])
  questions       Question[]
  participants    PollParticipant[]
  voteDrafts      VoteDraft[]
  legalCheck      PollLegalCheck?
  legalCheckLogs  PollLegalCheckLog[]
  properties      PollProperty[]
  eligibleMembers PollEligibleMember[]

  @@index([organizationId])
  @@index([boardId])
  @@index([state])
  @@index([createdBy])
  @@map("polls")
}

model Question {
  id           String    @id @default(cuid())
  text         String
  details      String?
  pollId       String    @map("poll_id")
  page         Int       @default(1)
  order        Int       @default(0)
  questionType String    @default("single-choice") @map("question_type") // "single-choice" | "multiple-choice"
  createdAt    DateTime  @default(now()) @map("created_at")
  archivedAt   DateTime? @map("archived_at")

  poll    Poll     @relation(fields: [pollId], references: [id], onDelete: Cascade)
  answers Answer[]
  votes   Vote[]
  voteDrafts VoteDraft[]

  @@index([pollId])
  @@index([pollId, page, order])
  @@map("questions")
}

model Answer {
  id         String    @id @default(cuid())
  text       String
  questionId String    @map("question_id")
  order      Int       @default(0)
  createdAt  DateTime  @default(now()) @map("created_at")
  archivedAt DateTime? @map("archived_at")

  question Question @relation(fields: [questionId], references: [id], onDelete: Cascade)
  votes    Vote[]
  voteDrafts VoteDraft[]

  @@index([questionId])
  @@map("answers")
}

model Vote {
  id         String   @id @default(cuid())
  questionId String   @map("question_id")
  answerId   String   @map("answer_id")
  userId     String   @map("user_id")
  userWeight Decimal  @default(1.0) @map("user_weight") @db.Decimal(15, 6)
  createdAt  DateTime @default(now()) @map("created_at")

  question Question @relation(fields: [questionId], references: [id], onDelete: Cascade)
  answer   Answer   @relation(fields: [answerId], references: [id], onDelete: Cascade)
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([questionId, userId, answerId])
  @@index([answerId])
  @@index([userId])
  @@map("votes")
}

model PollParticipant {
  id         String   @id @default(cuid())
  pollId     String   @map("poll_id")
  userId     String   @map("user_id")
  userWeight          Decimal  @default(1.0) @map("user_weight") @db.Decimal(15, 6)
  willingToSignProtocol Boolean? @map("willing_to_sign_protocol")
  snapshotAt          DateTime @default(now()) @map("snapshot_at")
  createdAt           DateTime @default(now()) @map("created_at")

  poll          Poll                           @relation(fields: [pollId], references: [id], onDelete: Cascade)
  user          User                           @relation(fields: [userId], references: [id], onDelete: Cascade)
  weightHistory ParticipantWeightHistory[]

  @@unique([pollId, userId])
  @@index([pollId])
  @@index([userId])
  @@map("poll_participants")
}

model ParticipantWeightHistory {
  id            String   @id @default(cuid())
  participantId String   @map("participant_id")
  pollId        String   @map("poll_id")
  userId        String   @map("user_id")
  oldWeight     Decimal  @db.Decimal(15, 6) @map("old_weight")
  newWeight     Decimal  @db.Decimal(15, 6) @map("new_weight")
  changedBy     String   @map("changed_by")
  reason        String?
  changedAt     DateTime @default(now()) @map("changed_at")

  participant    PollParticipant @relation(fields: [participantId], references: [id], onDelete: Cascade)
  changedByUser  User            @relation("WeightChangedBy", fields: [changedBy], references: [id])

  @@index([participantId])
  @@index([pollId])
  @@index([userId])
  @@index([changedBy])
  @@map("participant_weight_history")
}

model VoteDraft {
  id         String   @id @default(cuid())
  pollId     String   @map("poll_id")
  questionId String   @map("question_id")
  answerId   String   @map("answer_id")
  userId     String   @map("user_id")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  poll     Poll     @relation(fields: [pollId], references: [id], onDelete: Cascade)
  question Question @relation(fields: [questionId], references: [id], onDelete: Cascade)
  answer   Answer   @relation(fields: [answerId], references: [id], onDelete: Cascade)
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([pollId, questionId, userId, answerId])
  @@index([pollId])
  @@index([questionId])
  @@index([userId])
  @@index([pollId, userId])
  @@map("vote_drafts")
}

// Organization Join Parent Request
model OrganizationJoinParentRequest {
  id                String    @id @default(cuid())
  childOrgId        String    @map("child_org_id")
  parentOrgId       String    @map("parent_org_id")
  requestingAdminId String    @map("requesting_admin_id")
  handlingAdminId   String?   @map("handling_admin_id")
  message           String
  status            String    @default("pending") // "pending" | "accepted" | "rejected"
  rejectionReason   String?   @map("rejection_reason")
  createdAt         DateTime  @default(now()) @map("created_at")
  handledAt         DateTime? @map("handled_at")

  childOrg        Organization @relation("ChildOrgJoinParent", fields: [childOrgId], references: [id], onDelete: Cascade)
  parentOrg       Organization @relation("ParentOrgJoinParent", fields: [parentOrgId], references: [id], onDelete: Cascade)
  requestingAdmin User         @relation("RequestingAdmin", fields: [requestingAdminId], references: [id])
  handlingAdmin   User?        @relation("HandlingAdmin", fields: [handlingAdminId], references: [id], onDelete: SetNull)

  @@index([childOrgId])
  @@index([parentOrgId])
  @@index([requestingAdminId])
  @@index([handlingAdminId])
  @@map("organization_join_parent_requests")
}

// OTP Verification
model OtpVerification {
  id          String    @id @default(cuid())
  identifier  String    // phone number (E.164 format)
  channel     String    @default("sms") // "sms" | "email"
  code        String    // hashed code
  clientIp    String    @map("client_ip")
  userId      String    @map("user_id")
  attempts    Int       @default(0)
  maxAttempts Int       @default(5) @map("max_attempts")
  expiresAt   DateTime  @map("expires_at")
  verifiedAt  DateTime? @map("verified_at")
  createdAt   DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([identifier, channel])
  @@index([clientIp])
  @@index([expiresAt])
  @@index([userId])
  @@map("otp_verifications")
}

// Notification
model Notification {
  id        String    @id @default(cuid())
  userId    String    @map("user_id")
  type      String
  title     String
  body      String
  data      Json?
  readAt    DateTime? @map("read_at")
  createdAt DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, readAt])
  @@map("notifications")
}

// Privacy Audit Log
model UserPrivacyAuditLog {
  id               String   @id @default(cuid())
  userId           String   @map("user_id")
  allowFindByName  Boolean  @map("allow_find_by_name")
  allowFindByPhone Boolean  @map("allow_find_by_phone")
  allowFindByAddress Boolean @map("allow_find_by_address")
  changedAt        DateTime @default(now()) @map("changed_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("user_privacy_audit_log")
}

// Rate Limit Events (suspicious activity tracking)
model RateLimitEvent {
  id           String   @id @default(cuid())
  key          String
  limiterLabel String   @map("limiter_label")
  userId       String?  @map("user_id")
  createdAt    DateTime @default(now()) @map("created_at")

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([key])
  @@index([userId])
  @@index([limiterLabel])
  @@index([createdAt])
  @@map("rate_limit_events")
}

// User Block Status (append-only log)
model UserBlockStatus {
  id                    String   @id @default(cuid())
  userId                String   @map("user_id")
  status                String   // "blocked" | "unblocked"
  statusChangedBySuperadminId String   @map("status_changed_by_superadmin_id")
  reason                      String?
  createdAt                   DateTime @default(now()) @map("created_at")

  user            User @relation("BlockedUser", fields: [userId], references: [id], onDelete: Cascade)
  statusChangedBy User @relation("StatusChangedBySuperadmin", fields: [statusChangedBySuperadminId], references: [id])

  @@index([userId])
  @@index([userId, createdAt])
  @@map("user_block_statuses")
}

// IP Block Status (append-only log)
model IpBlockStatus {
  id                          String   @id @default(cuid())
  ipAddress                   String   @map("ip_address")
  status                      String   // "blocked" | "unblocked"
  statusChangedBySuperadminId String   @map("status_changed_by_superadmin_id")
  reason                      String?
  createdAt                   DateTime @default(now()) @map("created_at")

  statusChangedBy User @relation("IpBlockStatusChangedBy", fields: [statusChangedBySuperadminId], references: [id])

  @@index([ipAddress])
  @@index([ipAddress, createdAt])
  @@map("ip_block_statuses")
}

// Invitation Models
model Invitation {
  id             String    @id @default(cuid())
  organizationId String    @map("organization_id")
  boardId        String?   @map("board_id")
  inviterId      String    @map("inviter_id")
  inviteeId      String    @map("invitee_id")
  type           String    // "admin_invite" | "board_member_invite" | "member_invite"
  status         String    @default("pending") // "pending" | "accepted" | "declined" | "revoked"
  createdAt      DateTime  @default(now()) @map("created_at")
  handledAt      DateTime? @map("handled_at")

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  board        Board?       @relation(fields: [boardId], references: [id], onDelete: Cascade)
  inviter      User         @relation("InvitationInviter", fields: [inviterId], references: [id])
  invitee      User         @relation("InvitationInvitee", fields: [inviteeId], references: [id])

  @@index([organizationId, type, status])
  @@index([boardId, status])
  @@index([inviteeId, status])
  @@map("invitations")
}

// AI Legal Check Models
model PollLegalCheck {
  id          String      @id @default(cuid())
  pollId      String      @unique @map("poll_id")
  model       String
  annotations Json
  summary     Json
  overallRisk OverallRisk @map("overall_risk")
  totalIssues Int         @map("total_issues")
  checkedBy   String      @map("checked_by")
  checkedAt   DateTime    @default(now()) @map("checked_at")
  isStale     Boolean     @default(false) @map("is_stale")

  poll Poll @relation(fields: [pollId], references: [id], onDelete: Cascade)
  user User @relation("LegalChecks", fields: [checkedBy], references: [id], onDelete: Cascade)

  @@index([pollId])
  @@index([checkedBy])
  @@map("poll_legal_checks")
}

model PollLegalCheckLog {
  id          String   @id @default(cuid())
  pollId      String   @map("poll_id")
  checkedBy   String   @map("checked_by")
  checkedAt   DateTime @default(now()) @map("checked_at")
  inputTokens  Int     @default(0) @map("input_tokens")
  outputTokens Int     @default(0) @map("output_tokens")

  poll Poll @relation(fields: [pollId], references: [id], onDelete: Cascade)
  user User @relation("LegalCheckLogs", fields: [checkedBy], references: [id], onDelete: Cascade)

  @@index([checkedBy, checkedAt])
  @@index([pollId])
  @@map("poll_legal_check_logs")
}

// System Settings
model SystemSetting {
  key       String   @id
  value     String
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("system_settings")
}

// Property Models
model OrganizationProperty {
  id             String    @id @default(cuid())
  organizationId String    @map("organization_id")
  name           String
  address        String?
  sizeUnit       String    @map("size_unit")
  createdAt      DateTime  @default(now()) @map("created_at")
  archivedAt     DateTime? @map("archived_at")

  organization Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  assets       PropertyAsset[]
  polls        PollProperty[]

  @@index([organizationId])
  @@map("organization_properties")
}

model PropertyAsset {
  id          String    @id @default(cuid())
  propertyId  String    @map("property_id")
  name        String
  size        Decimal   @db.Decimal(15, 6)
  createdAt   DateTime  @default(now()) @map("created_at")
  archivedAt  DateTime? @map("archived_at")

  property   OrganizationProperty     @relation(fields: [propertyId], references: [id], onDelete: Cascade)
  ownerships PropertyAssetOwnership[]
  claims     PropertyClaim[]

  @@index([propertyId])
  @@map("property_assets")
}

/// IMPORTANT: a partial UNIQUE INDEX on (asset_id, user_id) WHERE
/// effective_until IS NULL AND user_id IS NOT NULL is enforced at the DB
/// level (see migration `20260427142323_unique_active_ownership_per_user`).
/// This prevents two ACTIVE ownership rows for the same registered user on
/// the same asset — would corrupt the weight calc and the per-asset
/// share-sum invariant. External placeholders (user_id IS NULL) and
/// historical rows (effective_until IS NOT NULL) are deliberately exempt
/// and may repeat freely. Prisma 7 cannot express partial unique indexes
/// via @@unique, so the constraint lives in the migration only.
model PropertyAssetOwnership {
  id                 String    @id @default(cuid())
  assetId            String    @map("asset_id")
  userId             String?   @map("user_id")
  externalOwnerLabel String?   @map("external_owner_label")
  share              Decimal   @db.Decimal(9, 8)
  effectiveFrom      DateTime  @default(now()) @map("effective_from")
  effectiveUntil     DateTime? @map("effective_until")
  createdAt          DateTime  @default(now()) @map("created_at")

  asset PropertyAsset @relation(fields: [assetId], references: [id], onDelete: Cascade)
  user  User?         @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([assetId, effectiveUntil])
  @@index([userId, effectiveUntil])
  @@map("property_asset_ownerships")
}

model PollProperty {
  pollId     String @map("poll_id")
  propertyId String @map("property_id")

  poll     Poll                 @relation(fields: [pollId], references: [id], onDelete: Cascade)
  property OrganizationProperty @relation(fields: [propertyId], references: [id], onDelete: Cascade)

  @@id([pollId, propertyId])
  @@index([pollId])
  @@map("poll_properties")
}

model PollEligibleMember {
  id         String   @id @default(cuid())
  pollId     String   @map("poll_id")
  userId     String   @map("user_id")
  snapshotAt DateTime @map("snapshot_at")
  createdAt  DateTime @default(now()) @map("created_at")

  poll Poll @relation(fields: [pollId], references: [id], onDelete: Cascade)
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([pollId, userId])
  @@index([pollId])
  @@map("poll_eligible_members")
}

model PropertyClaim {
  id             String    @id @default(cuid())
  organizationId String    @map("organization_id")
  userId         String    @map("user_id")
  assetId        String    @map("asset_id")
  status         String
  deniedReason   String?   @map("denied_reason")
  decidedBy      String?   @map("decided_by")
  decidedAt      DateTime? @map("decided_at")
  createdAt      DateTime  @default(now()) @map("created_at")

  organization  Organization  @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user          User          @relation("PropertyClaimUser", fields: [userId], references: [id], onDelete: Cascade)
  asset         PropertyAsset @relation(fields: [assetId], references: [id], onDelete: Cascade)
  decidedByUser User?         @relation("PropertyClaimDecidedBy", fields: [decidedBy], references: [id])
  attachments   PropertyClaimAttachment[]

  @@index([organizationId, status])
  @@index([userId])
  @@index([assetId])
  @@map("property_claims")
}

model PropertyClaimAttachment {
  id        String   @id @default(cuid())
  claimId   String   @map("claim_id")
  fileName  String   @map("file_name")
  mimeType  String   @map("mime_type")
  sizeBytes Int      @map("size_bytes")
  // bytea — kept inline for atomicity with the claim row. Capped at 10 MB by
  // domain validation; Postgres TOAST stores values >2 KB out of line, so this
  // does not slow row scans on PropertyClaim. Migrating to object storage
  // later means swapping `bytes` for a `url` column on this same table.
  bytes     Bytes
  createdAt DateTime @default(now()) @map("created_at")

  claim PropertyClaim @relation(fields: [claimId], references: [id], onDelete: Cascade)

  @@index([claimId])
  @@map("property_claim_attachments")
}

